import React from 'react';
import { ProjectCompletionType } from '@/types/projectTypes';

interface LoadProjectsYearlyProps {
  year: number;
  total: number;
  completedProjects: ProjectCompletionType[];
}
export default function LoadProjectsYearly({
  year,
  total,
  completedProjects,
}: LoadProjectsYearlyProps) {
  return <div>
    
  </div>;
}
